import java.util.Arrays;
public class SortArray {
    public static void main(String[] args) {
        int[] arr = {5, 2, 8, 1, 3};
        Arrays.sort(arr); // method sort -> sort the array in ascending order
        System.out.println(Arrays.toString(arr));
    }
}